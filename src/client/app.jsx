import React from "react";
import ReactDOM from "react-dom";
import { Main } from "./components/main/main";

/*
 Todo:
 - react-router
 - global state management (e.g. Redux) if needed
 */

ReactDOM.render(<Main />, document.getElementById("root"));
